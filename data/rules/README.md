# Rule packs

One JSON file per pack. Validated by `pnpm rules:validate`, which gates the build.
See `lib/rules/schema.ts` for the contract and `README.md` for what `verification`
means. No rule ships without a citation.
